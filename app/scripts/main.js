document.addEventListener("load", start);
let inputSearch = document.addEventListener(keyup, "#input-search");

function start() {
  preventFormSubmit();
  
}

function preventFormSubmi(){
  function handleFormSubmit(event) {
   
  }
  
}